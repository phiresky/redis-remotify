{
	"name": "@phiresky/redis-remotify",
	"version": "0.0.2",
	"description": "a tiny library for remote calls and events via redis",
	"main": "bin/remotify.js",
	"typings": "bin/remotify.d.ts",
	"author": "phiresky",
	"license": "MIT",
	"scripts": {
		"prepublish": "tsc",
		"precommit": "lint-staged"
	},
	"files": ["lib", "LICENSE"],
	"lint-staged": {
		"*.{json,ts,tsx,js,jsx}": ["prettier --write", "git add"]
	},
	"peerDependencies": {
		"redis": "^2.8.0"
	},
	"devDependencies": {
		"@types/debug": "^0.0.30",
		"@types/redis": "^2.8.4",
		"husky": "^0.14.3",
		"lint-staged": "^6.0.0"
	},
	"dependencies": {
		"debug": "^3.1.0"
	}
}
